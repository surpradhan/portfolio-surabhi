/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/portfolio-surabhi',
  assetPrefix: '/portfolio-surabhi',
};

export default nextConfig;
